# Руководство разработчика

## Требования к среде

- **ОС**: Windows 10 / 11 (x64)
- **IDE**: Visual Studio 2022 или JetBrains Rider
- **SDK**: .NET 8.0 SDK
- **Зависимости**: `libmpv-2.dll` (должен быть помещён в выходную директорию)

## Сборка и запуск

```powershell
# Восстановление зависимостей
dotnet restore

# Сборка (Debug)
dotnet build

# Запуск
dotnet run
```

> **Примечание**: Перед запуском убедитесь, что `libmpv-2.dll` находится в директории `bin\Debug\net8.0-windows\`, иначе программа завершится с ошибкой.

## Устранение неполадок

| Проблема | Возможная причина | Решение |
| :--- | :--- | :--- |
| **Крах при запуске** | Отсутствует `libmpv-2.dll` | Скачайте dll соответствующей архитектуры и поместите в директорию запуска |
| **Видео есть, звука нет** | Таймаут определения аудио | Нормальная стратегия оптимизации, попробуйте переключить дорожку или перезапустить |
| **EPG "Нет данных"** | Проблема с сетью или форматом | Проверьте доступность XMLTV URL и формат GZIP |
| **Настройки не сохраняются** | Нет прав доступа | Убедитесь, что у программы есть права на запись в директорию |

## Тестирование и вклад

### Процесс участия

1. **Fork** репозитория.
2. Создайте ветку: `git checkout -b feature/AmazingFeature`.
3. Закоммитьте изменения: `git commit -m 'feat: Add some AmazingFeature'` (следуйте [Conventional Commits](https://www.conventionalcommits.org/)).
4. Отправьте ветку: `git push origin feature/AmazingFeature`.
5. Создайте **Pull Request**.

### Стиль кода

- Придерживайтесь существующего стиля C# (K&R / Allman, см. .editorconfig).
- При изменении UI учитывайте адаптацию под тёмную и светлую темы.

### Производительность

- **CPU**: < 15% при 1080p (база i5-8250U).
- **RAM**: < 500MB при стабильном воспроизведении.
- **Запуск**: Холодный старт < 2 сек.
